@namespace demoVer.Shared
@inherits LayoutComponentBase

@implements IDisposable

@using demoVer.Models
@using System
@using System.IO
@using System.Text.Json
@using Newtonsoft.Json.Linq
@using demoVer.Utils
@using System.Globalization

@inject IWebHostEnvironment WebHostEnvironment
@inject IJSRuntime JS
@inject IDialogService DialogService
@inject GlobalVar globalVar
@inject HeartbeatService Heartbeat
@*每個要用MudBlazor的razor元件，都要引入如下元件*@
@*這邊還要引用MudBlazor可能是因為 _Imports.razor的引用不會給Shared內的razor檔案*@
@using MudBlazor
@using MudBlazor.Utilities
<MudThemeProvider/>
<MudPopoverProvider/>
<MudDialogProvider/>
<MudSnackbarProvider/>

@*
edit-container是用來綁以下javascript事件的 id : 
1.電腦右鍵
2.手機長按
*@
<div id=edit-container @onclick="CloseMenu">
@switch(currentCardInfo.card_Type)
{
    case CardType.Info:
        @if(showMenu)
        {
            <div class="custom-menu" style="top:@($"{menuY}px"); left:@($"{menuX}px");" @onclick:stopPropagation="true">
                <div class="custom-menu-item" @onclick="Edit_inCorrespondingStage">編輯</div>
                <div class="custom-menu-item" @onclick="Delete_inCorrespondingStage">刪除</div>
            </div>
            
        }
        @if(ConfirmDialog)
        {
            <div id="confirmDialog" class="custom-dialog-overlay">
                <div class="custom-dialog">
                    <div class="custom-dialog-content">
                        <div class="icon-container">
                            ⚠️
                        </div>
                        <p id="dialogMessage" class="dialog-message">@AlertMessage</p>
                    </div>
                    <div class="custom-dialog-actions">
                        <button class="btn cancel-btn" @onclick="Cancel">取消</button>
                        <button class="btn confirm-btn" @onclick="Confirm">確定</button>
                    </div>
                </div>
            </div>
        }

        switch(INFO_setting_stage)
        {
            case INFO_SETTING_STAGE.ADD_ROW_STAGE:
                <div class="edit-info-top">
                    <input class="input-edit" @bind=edit_CardName placeholder="輸入卡片名稱" style="text-align: center;"/>
                </div>
                <br>
                @* <div class="underline"></div> *@
                <table class="table no-text-select" id="table-settingInfoDataItem">
                    <thead>
                        <tr>
                            <th>圖示</th>
                            <th>內容設定</th>
                        </tr>
                    </thead>
                    <tbody>
                        @for(int i = 0 ; i < edit_INFO_DataItems.Count ; i ++)
                        {
                            var temp = edit_INFO_DataItems[i];
                            string iconPath;
                            iconPath = (string.IsNullOrEmpty(temp.iconPath)) ? "images/square.png" : temp.iconPath;
                            <tr>
                                <td>
                                    <button class="icon-button" @onclick="() => selectImg(temp)">
                                        <img src=@iconPath alt="waitImg"/>
                                    </button>
                                </td>
                                <td class="basic-td long-press-row" data-table="table-settingInfoDataItem" data-index="@i">
                                @* <td class="clickable-td" @onclick="() => AdjustValueCmdRow(temp)"> *@
                                    @{
                                        string hint = "";
                                        switch(temp.status)
                                        {
                                            case SettingStatus.YET:
                                                if(isMobile){hint = "長按此處編輯";}
                                                else{hint = "右鍵編輯";}
                                                break;
                                            case SettingStatus.PROGRESS:
                                                if(isMobile){hint = "未完成:長按此處編輯";}
                                                else{hint = "未完成:右鍵編輯";}
                                                break;
                                            case SettingStatus.DONE:
                                                hint = "設定完成";
                                                break;
                                            default:
                                                hint = "";
                                                break;
                                        }
                                    }
                                    @hint
                                </td>
                            </tr>
                        }
                    </tbody>
                </table>
                <div class="confirm-cancel-container">
                    <MudButton Class="no-transform"
                            OnClick="Add_INFO_DataItem"
                            Variant="Variant.Filled"
                            Color="Color.Primary"
                            StartIcon="@Icons.Material.Filled.Add"
                            Style="width: 120px;">
                        新增
                    </MudButton>
                    <MudButton Class="no-transform" 
                            OnClick="Confirm_INFO_DataItem"
                            Variant="Variant.Filled"
                            Color="Color.Success"
                            Style="width: 120px;">
                        確定
                    </MudButton>
                </div>
                break;
            case INFO_SETTING_STAGE.SELECT_ICON_STAGE:
                <div class="img-select-grid">
                    @foreach(var imgPath in imgRelativePaths)
                    {
                        <div class="img-select-icon">
                            <button class="icon-button" @onclick="() => selectedImgFile(imgPath)">
                                <img src=@imgPath alt="waiting"/>
                            </button>
                        </div>
                    }
                </div>
                break;
            case INFO_SETTING_STAGE.SETTING_VALUE_AND_CMD_STAGE:
                <div class="edit-info-top">
                    <h3>數值與命令設定</h3>
                </div>
                <table class="table">
                    <tbody>
                        @if(currentEdit_INFO_DataFormat_ref != null)
                        {
                            <tr>
                                <th>設備選擇</th>
                                <td class="clickable-td" @onclick="() => selectMachine()">
                                    @(string.IsNullOrEmpty(currentEdit_INFO_DataFormat_ref.MachineName) ? "設定" : currentEdit_INFO_DataFormat_ref.MachineName)
                                </td>
                            </tr>
                            <tr>
                                <th>命令選擇</th>
                                <td class="clickable-td" @onclick="() => selectCMD()">
                                    @(string.IsNullOrEmpty(currentEdit_INFO_DataFormat_ref.Cmd) ? "設定" : currentEdit_INFO_DataFormat_ref.Cmd)
                                </td>
                            </tr>
                            <tr>
                                <th>數值名稱</th>
                                <td>
                                    <input @bind="currentEdit_INFO_DataFormat_ref.ValueName" class="td-input"  style="text-align: center;"/>
                                </td>
                            </tr>
                            <tr>
                                <th>單位</th>
                                <td style="text-align: center;">
                                    @{
                                        string Uint_To_show = "";
                                        Uint_To_show = string.IsNullOrEmpty(currentEdit_INFO_DataFormat_ref.Unit) ? "Please select a CMD" : currentEdit_INFO_DataFormat_ref.Unit;
                                        @* if(string.IsNullOrEmpty(currentEdit_INFO_DataFormat_ref.Cmd))
                                        {
                                            Uint_To_show = "請先選擇命令";
                                        }
                                        else
                                        {
                                            if(string.IsNullOrEmpty(currentEdit_INFO_DataFormat_ref.Unit))
                                            {
                                                Uint_To_show = "設定";
                                            }
                                            else
                                            {
                                                Uint_To_show = currentEdit_INFO_DataFormat_ref.Unit;
                                            }
                                        } *@
                                    }
                                    @Uint_To_show
                                </td>
                            </tr>
                        }
                    </tbody>
                </table>
                <div class="confirm-cancel-container">
                     <MudButton OnClick="confirm_INFO_DataFormat_ValueSetting"
                            Variant="Variant.Filled"
                            Color="Color.Success">
                        確定
                    </MudButton>
                    @* <button @onclick="confirm_INFO_DataFormat_ValueSetting">確認</button> *@
                </div>
                break;
            case INFO_SETTING_STAGE.MACHINE_SELECTION_STAGE:
                <MudPaper Elevation="2" Class="pa-2 rounded" Style="width: 250px; max-height: 240px; overflow-y: auto;">
                    <MudList T="string" Dense="true" Hover="true">
                        @foreach (var (addr, modelName) in deviceAddr_List)
                        {
                            <MudListItem T="string"
                                        @onclick="() => clickMachineName(addr, modelName)"
                                        Style="cursor: pointer;">
                                <MudListItemIcon>
                                    <MudIcon Icon="@Icons.Material.Filled.KeyboardCommandKey" />
                                </MudListItemIcon>
                                <MudText Class="ml-2">@($"[{addr}]: {modelName}")</MudText>
                            </MudListItem>
                        }
                    </MudList>
                </MudPaper>
                
                break;
            case INFO_SETTING_STAGE.CMD_SELECTION_STAGE:
                <MudPaper Elevation="2" Class="pa-2 rounded" Style="width: 300px; max-height: 240px; overflow-y: auto;">
                    <MudList T="string" Dense="true" Hover="true">
                        @foreach (var (cmd, unit) in show_cmd_unit_Dict_ref)
                        {
                            <MudListItem T="string"
                                        @onclick="() => clickCmd(cmd, unit)"
                                        Style="cursor: pointer;">
                                <MudListItemIcon>
                                    <MudIcon Icon="@Icons.Material.Filled.Code" />
                                </MudListItemIcon>
                                <MudText Class="ml-2">@cmd</MudText>
                            </MudListItem>
                        }
                    </MudList>
                </MudPaper>
                break;
            case INFO_SETTING_STAGE.UNIT_SELECTION_STAGE:
                <MudPaper Elevation="2" Class="pa-2 rounded" Style="width: 200px; max-height: 240px; overflow-y: auto;">
                    <MudList T="string" Dense="true" Hover="true">
                        @foreach (var unit in UnitLists)
                        {
                            <MudListItem T="string"
                                        @onclick="() => clickUnit(unit)"
                                        Style="cursor: pointer;">
                                <MudListItemIcon>
                                    <MudIcon Icon="@Icons.Material.Filled.Sensors" />
                                </MudListItemIcon>
                                <MudText Class="ml-2">@unit</MudText>
                            </MudListItem>
                        }
                    </MudList>
                </MudPaper>
                break;
            
            case INFO_SETTING_STAGE.ADD_VALUE_AND_CMD_ROW_STAGE:
                <div style="max-height: 300px; overflow-y: auto;">
                    <h4>@(isMobile? "長按編輯" : "右鍵編輯")</h4>
                    <table class="table no-text-select" id="table-settingInfoDataFormat">
                        <thead>
                            <th>數值名稱</th>
                            <th>單位</th>
                        </thead>
                        <tbody @onclick:stopPropagation="true">
                            @for(int i = 0 ; i < currentEdit_INFO_DataItem_ref.INFO_DataFormats.Count ; i ++)
                            {
                                var tempDataFormat = currentEdit_INFO_DataItem_ref.INFO_DataFormats[i];
                                <tr class="long-press-row" data-table="table-settingInfoDataFormat" data-index="@i">
                                    <td class="basic-td">
                                        @(string.IsNullOrEmpty(tempDataFormat.ValueName) ? "" : tempDataFormat.ValueName)
                                    </td>
                                    <td>
                                        @(string.IsNullOrEmpty(tempDataFormat.Unit) ? "" : tempDataFormat.Unit)
                                    </td>
                                </tr>
                            }
                        </tbody>
                    </table>
                </div>
                <div class="confirm-cancel-container">
                    <MudButton OnClick="AddRow_ValueAndCmd"
                            Variant="Variant.Filled"
                            Color="Color.Primary"
                            StartIcon="@Icons.Material.Filled.Add"
                            Style="width: 120px;">
                        新增
                    </MudButton>
                    <MudButton OnClick="confirm_INFO_DataFormats_Setting"
                                Variant="Variant.Filled"
                                Color="Color.Success"
                                Style="width: 120px;">
                        確認
                    </MudButton>
                </div>
                break;
            default:
                break;
        }
        
        break;
    case CardType.Chart:
        @if(showMenu)
        {
            <div class="custom-menu" style="top:@($"{menuY}px"); left:@($"{menuX}px");" @onclick:stopPropagation="true">
                <div class="custom-menu-item" @onclick="Edit_inCorrespondingStage">編輯</div>
                <div class="custom-menu-item" @onclick="Delete_inCorrespondingStage">刪除</div>
            </div>   
        }
        @if(ConfirmDialog)
        {
            <div id="confirmDialog" class="custom-dialog-overlay">
                <div class="custom-dialog">
                    <div class="custom-dialog-content">
                        <div class="icon-container">
                            ⚠️
                        </div>
                        <p id="dialogMessage" class="dialog-message">@AlertMessage</p>
                    </div>
                    <div class="custom-dialog-actions">
                        <button class="btn cancel-btn" @onclick="Cancel">取消</button>
                        <button class="btn confirm-btn" @onclick="Confirm">確定</button>
                    </div>
                </div>
            </div>
        }
        switch(CHART_setting_stage)
        {
            case CHART_SETTING_STAGE.OVERVIEW:
                <h5>編輯 : 圖表</h5>
                <MudTextField T="string" @bind-Value="temp_chartTitle" Label="Y-Title" Required="true" Variant="Variant.Outlined"/>
                <table class="table no-text-select" id="table-ChartSetting">
                    <thead>
                        <tr>
                            <th>命令(單位)</th>
                            <th>Y軸標題-左/右-顏色</th>
                        </tr>
                    </thead>
                    <tbody>
                        @for(int i = 0 ; i < edit_ChartSetting_Clone.chart_single_data_lines.Count ; i ++)
                        {
                            var temp = edit_ChartSetting_Clone.chart_single_data_lines[i];
                            <tr>
                                <td class="basic-td long-press-row" data-table="table-ChartSetting" data-index="@i">
                                    @{
                                        string hint_cmd = "";
                                        switch(temp.status)
                                        {
                                            case SettingStatus.YET:
                                                if(isMobile){hint_cmd = "長按此處編輯";}
                                                else{hint_cmd = "右鍵編輯";}
                                                break;
                                            case SettingStatus.PROGRESS:
                                                if(isMobile){hint_cmd = "未完成:長按此處編輯";}
                                                else{hint_cmd = "未完成:右鍵編輯";}
                                                break;
                                            case SettingStatus.DONE:
                                                hint_cmd = temp.Cmd + $"({temp.Unit})";
                                                break;
                                            default:
                                                hint_cmd = "";
                                                break;
                                        }
                                    }
                                    @hint_cmd
                                </td>
                                <td class="basic-td long-press-row" data-table="table-ChartSetting" data-index="@i">
                                    @{
                                        string hint = "";
                                        string hint_color = "#000000";
                                        switch(temp.status)
                                        {
                                            case SettingStatus.YET:
                                                if(isMobile){hint = "長按此處編輯";}
                                                else{hint = "右鍵編輯";}
                                                break;
                                            case SettingStatus.PROGRESS:
                                                if(isMobile){hint = "未完成:長按此處編輯";}
                                                else{hint = "未完成:右鍵編輯";}
                                                break;
                                            case SettingStatus.DONE:
                                                if(temp.Y_axis_selection == true){hint = $"{edit_ChartSetting_Clone.Y_left_Title}-左";}
                                                else if(temp.Y_axis_selection == false){hint = $"{edit_ChartSetting_Clone.Y_right_Title}-右";}
                                                hint_color = temp.color;
                                                break;
                                            default:
                                                hint = "";
                                                break;
                                        }
                                    }
                                    
                                    <div class="display-show-setting">
                                        <span>@hint</span>
                                        <div style="width: 16px; height: 16px; border-radius: 4px; background-color: @hint_color; border: 1px solid #ccc;"></div>
                                    </div>
                                </td>
                            </tr>
                        }
                    </tbody>
                </table>
                <div class="confirm-cancel-container">
                    <MudButton Class="no-transform"
                            OnClick="Add_Curve"
                            Variant="Variant.Filled"
                            Color="Color.Primary"
                            StartIcon="@Icons.Material.Filled.Add"
                            Style="width: 120px;">
                        新增
                    </MudButton>
                    <MudButton Class="no-transform" 
                            OnClick="Confirm_CurveSetting"
                            Variant="Variant.Filled"
                            Color="Color.Success"
                            Style="width: 120px;">
                        確定
                    </MudButton>
                </div>
                break;
            case CHART_SETTING_STAGE.DISPLAY_SETTING:
                <div class="chart-display-setting">
                    <h3>顯示設定</h3>
                    <div class="chartForm">
                        @{
                            string paper_style = (isMobile) ? "" : "width: 400px; max-height: 400px;" ;
                            <MudPaper Elevation="2" Class="pa-2 rounded" Style="@paper_style">
                                <MudForm @ref="form" @bind-IsValid="@success">
                                    <MudTextField T="string" @bind-Value="temp_Y_singleTitle" Label="Y-Title" Required="true" RequiredError="Y-Title is required!" />
                                    <MudRadioGroup T="bool?" @bind-Value="blank_singleDataLine.Y_axis_selection" Required="true" RequiredError="Y-axis is required!">
                                        <MudRadio T="bool?" Value="@(true)">Left-axis</MudRadio>
                                        <MudRadio T="bool?" Value="@(false)">Right-axis</MudRadio>
                                    </MudRadioGroup>
                                    <MudColorPicker Label="Data-Color" @bind-Text="blank_singleDataLine.color" ColorPickerView="ColorPickerView.Palette" Style="@($"color: {blank_singleDataLine.color};")" Required="true"/>
                                    <MudButton Variant="Variant.Filled" Color="Color.Primary" Class="ml-auto" OnClick="Confirm_SingleDataLine_setting">確定</MudButton>
                                </MudForm>
                            </MudPaper>
                        }
                    </div>  
                    @if(!string.IsNullOrEmpty(chartForm_errorMessage))
                    {
                        <MudText Class="mud-error-text mt-2">@chartForm_errorMessage</MudText>
                    }
                </div>
                break;
            case CHART_SETTING_STAGE.SELECT_CMD_STAGE:
                <MudPaper Elevation="2" Class="pa-2 rounded" Style="width: 200px; max-height: 240px; overflow-y: auto;">
                    <MudList T="string" Dense="true" Hover="true">
                        @foreach (var item in cmdDict)
                        {
                            <MudListItem T="string"
                                        @onclick="() => clickCmd_CHART(item.Key)"
                                        Style="cursor: pointer;">
                                <MudListItemIcon>
                                    <MudIcon Icon="@Icons.Material.Filled.Code" />
                                </MudListItemIcon>
                                <MudText Class="ml-2">@item.Key</MudText>
                            </MudListItem>
                        }
                    </MudList>
                </MudPaper>
                break;
            case CHART_SETTING_STAGE.UNIT_SELECTION_STAGE:
                <MudPaper Elevation="2" Class="pa-2 rounded" Style="width: 200px; max-height: 240px; overflow-y: auto;">
                    <MudList T="string" Dense="true" Hover="true">
                        @foreach (var unit in UnitLists)
                        {
                            <MudListItem T="string"
                                        @onclick="() => clickUnit_CHART(unit)"
                                        Style="cursor: pointer;">
                                <MudListItemIcon>
                                    <MudIcon Icon="@Icons.Material.Filled.Sensors" />
                                </MudListItemIcon>
                                <MudText Class="ml-2">@unit</MudText>
                            </MudListItem>
                        }
                    </MudList>
                </MudPaper>
                break;
            default:
                break;
            
        }
        
        break;
    case CardType.Status:
        <h3>編輯 : 連接狀態</h3>
        switch(STATUS_setting_stage)
        {
            case STATUS_SETTING_STAGE.OVERVIEW:
                <table class="table no-text-select" id="table-linkingStatus">
                    <colgroup>
                        <col style="width: 20%;">
                        <col style="width: 30%;">
                        <col style="width: 60%;">
                    </colgroup>
                    <thead>
                        <tr>
                            <th>No.</th>
                            <th>圖示</th>
                            <th>文字</th>
                        </tr>
                    </thead>
                    <tbody>
                        @for(int i = 0 ; i < 4 ; i ++)
                        {
                            var temp = edit_Link_STATUS_Pairs[i];
                            if(temp == null)
                            {
                                Console.WriteLine($"temp is null!!!!");
                                return;
                            }
                            string iconPath = (string.IsNullOrEmpty(temp.iconPath)) ? "images/square.png" : temp.iconPath;
                            <tr>
                                <td>
                                    @(i+1)
                                </td>
                                <td>
                                    <button class="icon-button" @onclick="() => selectImg_LINK_STATUS(temp)">
                                        <img src=@iconPath alt="waitImg"/>
                                    </button>
                                </td>
                                <td>
                                    <input @bind="temp.Label" class="td-input" style="text-align: center;"/>
                                </td>
                            </tr>
                        }
                    </tbody>
                </table>
                <MudButton Class="no-transform" 
                        OnClick="Confirm_LINK_STATUS"
                        Variant="Variant.Filled"
                        Color="Color.Success"
                        Style="width: 120px;">
                    確定
                </MudButton>
                break;
            case STATUS_SETTING_STAGE.SELECT_ICON_STAGE:
                <div class="img-select-grid">
                    @foreach(var imgPath in imgRelativePaths)
                    {
                        <div class="img-select-icon">
                            <button class="icon-button" @onclick="() => selectedImgFile_LINK_STATUS(imgPath)">
                                <img src=@imgPath alt="waiting"/>
                            </button>
                        </div>
                    }
                </div>
                break;

            default:
                break;
        }
        
        break;
    default:
        break;
}


</div>


@code{
    
    //For log
    private string _category = "";
    //HeartbeatCounter
    private int HeartBeatCounter = 0;
    

    #region Common_Variables_and_Functions
    [Parameter] public CardInfo currentCardInfo {get; set;}
    [Parameter] public EventCallback<CardInfo> OnCardInfoUpdated {get; set;}
    private DotNetObjectReference<object>? objRef;

    private bool _isFirstParameterSet = true;
    private string imgFolderPath = "";
    private List<string> imgRelativePaths = new List<string>();
    private bool isMobile = false;
    private Dictionary<string, string> oneDevice_cmd_unit_Dict = new Dictionary<string, string>();
    private Dictionary<string, string> Summary_cmd_unit_Dict = new Dictionary<string, string>();
    private Dictionary<string, string> show_cmd_unit_Dict_ref;
    private JObject cmdDict;
    string Cmd_filePath = "CMD_and_Unit.json";
    private List<string> UnitLists;

    
    
    private async Task OnHeartbeatTick()
    {
        if(HeartBeatCounter == 0)
        {
            refreshDevicesList();
        }
        HeartBeatCounter = (HeartBeatCounter + 1) % 3;
    }

    protected override void OnInitialized()
    {
        _category = GetType().FullName!;

        Heartbeat.OnTick += OnHeartbeatTick;

        Console.WriteLine("🚀 元件初始化：OnInitialized 被呼叫");
        // 初始化邏輯，例如建立預設值、讀取 localStorage、設定變數等
        imgFolderPath = Path.Combine(WebHostEnvironment.WebRootPath, "images/user_selections");
        //getCMDs();
        InitSummaryCMDs();
        getImgFiles();
    }

    public void Dispose()
    {
        Heartbeat.OnTick -= OnHeartbeatTick;
        objRef?.Dispose();
    }


    protected override async Task OnAfterRenderAsync(bool firstRender)
    {
        if(firstRender)
        {
            objRef = DotNetObjectReference.Create<object>(this);
            await JS.InvokeVoidAsync("registerGroupedTableTouchAndRightClick", "edit-container", objRef);
            isMobile = await JS.InvokeAsync<bool>("checkIsMobile");
        }
    }

    protected override void OnParametersSet()
    {
        if (_isFirstParameterSet)
        {
            _isFirstParameterSet = false;

            // ✅ 第一次接收到 [Parameter] 時要執行的邏輯
            Console.WriteLine("🚀 第一次接收到參數 currentCardInfo！");
            if(!string.IsNullOrEmpty(currentCardInfo.cardName))
            {
                Console.WriteLine("[EditCard] : currentCardInfo.cardName = " + currentCardInfo.cardName);
                edit_CardName = currentCardInfo.cardName;

                switch(currentCardInfo.card_Type)
                {
                    case CardType.Info:
                        edit_INFO_DataItems = currentCardInfo.Info_DataItems; //edit_INFO_DataItems 與 currentCardInfo.Info_DataItems 同一塊記憶體
                        Console.WriteLine("[OnParameterSet]CardType.Info");
                        break;
                    case CardType.Chart:
                        edit_ChartSetting_Clone = currentCardInfo.Chart_Setting.DeepClone(); //edit_ChartSetting_Clone 與 currentCardInfo.Chart_Setting 不同塊記憶體
                        temp_chartTitle = edit_ChartSetting_Clone.ChartTitle;

                        if(edit_ChartSetting_Clone.chart_single_data_lines.Count == 0)
                        {
                            edit_ChartSetting_Clone.chart_single_data_lines = new List<CHART_SINGLE_DATA_LINE>();
                        }
                        break;
                    case CardType.Status:
                        edit_Link_STATUS_Pairs = currentCardInfo.Link_STATUS_Pairs?.Select(pair => pair.DeepClone()).ToList(); //edit_Link_STATUS_Pairs 與 currentCardInfo.Link_STATUS_Pairs 不同記憶體
                        break;
                    default:
                        break;
                }
            }
        }
    }

    [JSInvokable]
    public void JS_Console(string _debug_message)
    {
        Console.WriteLine(_debug_message);
    }

    private void getImgFiles()
    {
        if(Directory.Exists(imgFolderPath))
        {
            
            imgRelativePaths = Directory.GetFiles(imgFolderPath)
                                    .Select(path => "images/user_selections/" + Path.GetFileName(path))
                                    .ToList();
            foreach(var names in imgRelativePaths)
            {
                Console.WriteLine(names);
            }
        }
        else
        {
            Console.WriteLine("FAIL : getImgFiles");
        }
    }

    private void InitSummaryCMDs()
    {
        Summary_cmd_unit_Dict["Number of INV"]      = "Unit";
        Summary_cmd_unit_Dict["Input Voltage"]      = "VAC";
        Summary_cmd_unit_Dict["Input Frequency"]    = "Hz";
        Summary_cmd_unit_Dict["Output Voltage"]     = "VAC";
        Summary_cmd_unit_Dict["Output Frequency"]   = "Hz";
        Summary_cmd_unit_Dict["Output Current"]     = "A";
        Summary_cmd_unit_Dict["Load Percentage"]    = "%";
        Summary_cmd_unit_Dict["Battery Voltage"]    = "V";
        Summary_cmd_unit_Dict["Battery Level"]      = "%";
        Summary_cmd_unit_Dict["Battery Time Left"]  = "H";
    }

    private async Task getCMDs()
    {
        //oneDevice_cmd_unit_Dict //Dictionary <string, string>
        try
        {
            //清空 cmd_uint_Dict 的內容
            
            if(currentSelected_Addr == 9999) // Selecting "Summary"
            {
                show_cmd_unit_Dict_ref = Summary_cmd_unit_Dict;

                AppLogger.Log_To_File_log(_category, $"[EditCard][getCMDs] Select : addr = {currentSelected_Addr}, mdlName = Summary", AppLogLevel.Trace);
            }
            else if(currentSelected_Addr < 256)
            {
                oneDevice_cmd_unit_Dict.Clear();
                oneDevice_cmd_unit_Dict = globalVar.Get_oneDevice_Cmd_unit_Dict(currentSelected_Addr);

                show_cmd_unit_Dict_ref = oneDevice_cmd_unit_Dict;
                
                AppLogger.Log_To_File_log(_category, $"[EditCard][getCMDs] Select : addr = {currentSelected_Addr}, mdlName = {currentEdit_INFO_DataFormat_ref.MachineName}", AppLogLevel.Trace);
            }
            else
            {
                AppLogger.Log_To_File_log(_category, $"[EditCard][getCMDs] Got wrong addr : {currentSelected_Addr}", AppLogLevel.Information);
            }
        }
        catch(Exception e)
        {
            AppLogger.Log_To_File_log(_category, $"[EditCard][getCMDs] Error : {e}", AppLogLevel.Error);
        }
        

        @* if(File.Exists(Cmd_filePath))
        {
            string json = File.ReadAllText(Cmd_filePath);
            cmdDict = JObject.Parse(json);

            var json = await File.ReadAllTextAsync(Cmd_filePath);
            cmdDict = JsonSerializer.Deserialize<Dictionary<string, object>>(json);

            var jsonString = JsonSerializer.Serialize(cmdDict, new JsonSerializerOptions { WriteIndented = true });
            Console.WriteLine(jsonString); 

        } *@
    }
    #endregion Common_Variables_and_Functions


    #region INFO_CARD_Variables_and_Functions
    private INFO_SETTING_STAGE INFO_setting_stage = INFO_SETTING_STAGE.ADD_ROW_STAGE;
    private string title;
    private INFO_DataItem currentEdit_INFO_DataItem_ref; //reference, modify in memory
    private INFO_DataItem temp_INFO_DataItem_Backend;
    private INFO_DataFormat currentEdit_INFO_DataFormat_ref; //This is point to the one DataFormat of temp_INFO_DataItem_Backend.INFO_DataFormats
    private uint currentSelected_Addr;
    private string edit_CardName;
    private bool showMenu = false;
    private double menuX;
    private double menuY;
    private int InfoData_index; //紀錄 InfoDataItem, InfoDataFormat 被選擇到的index，依照Stage而代表不同的index

    private bool ConfirmDialog = false;
    private string AlertMessage = "";
    private Action? ConfirmCallback;

    private List<(uint, string)> deviceAddr_List = new List<(uint, string)>()
    {
        
    };

    private List<INFO_DataItem> edit_INFO_DataItems = new List<INFO_DataItem>
    {

    };

    private void refreshDevicesList()
    {

        try
        {
            deviceAddr_List = globalVar.Get_addr_and_ModelNames_List();
            deviceAddr_List.Add((9999, "Summary"));
        }
        catch(Exception e)
        {
            AppLogger.Log_To_File_log(_category, $"[EditCard][refreshDevicesList] Error : {e}", AppLogLevel.Error);
        }
        
        
    }

    [JSInvokable]
    public void OnShowContextMenu(string table, int index, double x, double y)
    {
        menuX = x;
        menuY = y;
        showMenu = true;
        Console.WriteLine($"[OnShowContextMenu] table : {table}, index : {index}");
        switch(currentCardInfo.card_Type)
        {
            case CardType.Info:
                InfoData_index = index;
                break;
            case CardType.Chart:
                SingleLine_index = index;
                break;
            case CardType.Status:
                break;
            default:
                break;
        }
        StateHasChanged();
    }

    private void ShowDialog(string _message, Action _ConfirmAction)
    {
        AlertMessage = _message;
        ConfirmCallback = _ConfirmAction;
        ConfirmDialog = true;
    }

    private void Confirm()
    {
        ConfirmDialog = false;
        ConfirmCallback?.Invoke();
    }

    private void Cancel()
    {
        ConfirmDialog = false;
    }
    
    private void DeleteAction_In_ADD_ROW_STAGE()
    {
        Console.WriteLine("執行刪除");
        if(InfoData_index >= 0 && InfoData_index < currentCardInfo.Info_DataItems.Count)
        {
            currentCardInfo.Info_DataItems.RemoveAt(InfoData_index);
        }
    }

    private void DeleteAction_In_ADD_VALUE_AND_CMD_ROW_STAGE()
    {
        Console.WriteLine("執行刪除");
        if(InfoData_index >= 0 && InfoData_index < currentEdit_INFO_DataItem_ref.INFO_DataFormats.Count)
        {
            currentEdit_INFO_DataItem_ref.INFO_DataFormats.RemoveAt(InfoData_index);
        }
    }

    private void CloseMenu()
    {
        if(showMenu)
        {
            showMenu = false;
        }
    }

    private void Edit_inCorrespondingStage()
    {
        showMenu = false;
        Console.WriteLine("點了選項1");
        switch(currentCardInfo.card_Type)
        {
            case CardType.Info:
                switch(INFO_setting_stage)
                {
                    case INFO_SETTING_STAGE.ADD_ROW_STAGE:
                        var tempINFO_DataItem = edit_INFO_DataItems[InfoData_index];
                        AdjustValueCmdRow(tempINFO_DataItem);
                        break;
                    case INFO_SETTING_STAGE.ADD_VALUE_AND_CMD_ROW_STAGE:
                        var tempDataFormat = currentEdit_INFO_DataItem_ref.INFO_DataFormats[InfoData_index];
                        setValueAndCmd(tempDataFormat);
                        break;
                    default:
                        break;
                }
                break;
            case CardType.Chart:
                switch(CHART_setting_stage)
                {
                    case CHART_SETTING_STAGE.OVERVIEW:
                        //reset blank_singleDataLine
                        blank_singleDataLine.Cmd = null;
                        blank_singleDataLine.Unit = null;
                        blank_singleDataLine.Y_axis_selection = null;
                        blank_singleDataLine.color = null;
                        temp_Y_singleTitle = null;
                        CHART_setting_stage = CHART_SETTING_STAGE.SELECT_CMD_STAGE;
                        break;
                    default:
                        break;
                }
                break;
            case CardType.Status:
                break;
            default:
                break;
        }
        
    }

    private void Delete_inCorrespondingStage()
    {
        showMenu = false;
        Console.WriteLine("點了選項2");
         switch(currentCardInfo.card_Type)
        {
            case CardType.Info:
                switch(INFO_setting_stage)
                {
                    case INFO_SETTING_STAGE.ADD_ROW_STAGE:
                        ShowDialog("確定要刪除?", DeleteAction_In_ADD_ROW_STAGE);
                        break;
                    case INFO_SETTING_STAGE.ADD_VALUE_AND_CMD_ROW_STAGE:
                        Console.WriteLine("[Delete_inCorrespondingStage] Stage = INFO_SETTING_STAGE.ADD_VALUE_AND_CMD_ROW_STAGE");
                        ShowDialog("確定要刪除?", DeleteAction_In_ADD_VALUE_AND_CMD_ROW_STAGE);
                        break;
                    default:
                        break;
                }
                break;
            case CardType.Chart:
                switch(CHART_setting_stage)
                {
                    case CHART_SETTING_STAGE.OVERVIEW:
                        Console.WriteLine("[DELETE] CHART_SETTING_STAGE.OVERVIEW");
                        ShowDialog("確定要刪除?", DeleteAction_In_CHART_OVERVIEW_STAGE);
                        break;
                    default:
                        break;
                }
                break;
            case CardType.Status:
                break;
            default:
                break;
        }
        
    }

    private void Add_INFO_DataItem()
    {
        edit_INFO_DataItems.Add(new INFO_DataItem());
    }

    private void selectImg(INFO_DataItem _temp_Info_DataItem)
    {
        currentEdit_INFO_DataItem_ref = _temp_Info_DataItem; 
        INFO_setting_stage = INFO_SETTING_STAGE.SELECT_ICON_STAGE;
    }

    private void selectedImgFile(string _imgPath)
    {
        Console.WriteLine(_imgPath);

        currentEdit_INFO_DataItem_ref.iconPath = _imgPath;
        INFO_setting_stage = INFO_SETTING_STAGE.ADD_ROW_STAGE;
    }

    private void AdjustValueCmdRow(INFO_DataItem _temp_Info_DataItem)
    {
        Console.WriteLine("AdjustValueCmdRow");

        //如果是已經存在的 currentEdit_INFO_DataItem，則

        currentEdit_INFO_DataItem_ref = _temp_Info_DataItem;
        
        INFO_setting_stage = INFO_SETTING_STAGE.ADD_VALUE_AND_CMD_ROW_STAGE;
    }

    private void AddRow_ValueAndCmd()
    {
        currentEdit_INFO_DataItem_ref.INFO_DataFormats.Add(new INFO_DataFormat());
    }

    private void setValueAndCmd(INFO_DataFormat _temp_DataFormat)
    {
        Console.WriteLine("setValueAndCmd");
        currentEdit_INFO_DataFormat_ref = _temp_DataFormat;
        try
        {
            if(!string.IsNullOrEmpty(currentEdit_INFO_DataFormat_ref.MachineName))
            {
                AppLogger.Log_To_File_log(_category, $"[EditCard][setValueAndCmd] ModelName = {currentEdit_INFO_DataFormat_ref.MachineName}", AppLogLevel.Trace);
                if(TryParseAddr_fromAddrModelName(currentEdit_INFO_DataFormat_ref.MachineName, out uint addr))
                {
                    AppLogger.Log_To_File_log(_category, $"[EditCard][setValueAndCmd] ModelName = {currentEdit_INFO_DataFormat_ref.MachineName}, addr = {addr}", AppLogLevel.Trace);
                    
                    currentSelected_Addr = addr;

                    getCMDs();
                }
            }
            else
            {
                AppLogger.Log_To_File_log(_category, $"[EditCard][setValueAndCmd] ModelName = null", AppLogLevel.Trace);
            }
        }
        catch(Exception e)
        {
            AppLogger.Log_To_File_log(_category, $"[EditCard][setValueAndCmd] Error : {e}");
        }
        

        INFO_setting_stage = INFO_SETTING_STAGE.SETTING_VALUE_AND_CMD_STAGE;
    }

    private void selectMachine()
    {
        Console.WriteLine("selectMachine");
        INFO_setting_stage = INFO_SETTING_STAGE.MACHINE_SELECTION_STAGE;
    }

    private void selectCMD()
    {
        Console.WriteLine("selectCMD");
        if(string.IsNullOrEmpty(currentEdit_INFO_DataFormat_ref.MachineName)) return; 

        

        getCMDs();
        INFO_setting_stage = INFO_SETTING_STAGE.CMD_SELECTION_STAGE;
    }

    private void selectUnit()
    {
        Console.WriteLine("selectUnit");
        if(!string.IsNullOrEmpty(currentEdit_INFO_DataFormat_ref.Cmd))
        {
            Console.WriteLine("selectUnit : currentEdit_INFO_DataFormat_ref.Cmd = " + currentEdit_INFO_DataFormat_ref.Cmd);
            if(cmdDict.ContainsKey(currentEdit_INFO_DataFormat_ref.Cmd))
            {
                var unitToken = cmdDict[currentEdit_INFO_DataFormat_ref.Cmd]["Unit"];
                if(unitToken != null)
                {
                    UnitLists = cmdDict[currentEdit_INFO_DataFormat_ref.Cmd]["Unit"].ToObject<List<string>>();
                }
            }
            INFO_setting_stage = INFO_SETTING_STAGE.UNIT_SELECTION_STAGE;
        }
        else
        {
            Console.WriteLine("selectUnit : 尚未選擇CMD (這邊可以跳個Alert框)");
        }
    }

    private bool TryParseAddr_fromAddrModelName(string _addrModelName, out uint value)
    {
        value = 0;
        if(string.IsNullOrEmpty(_addrModelName)) return false;
        AppLogger.Log_To_File_log(_category, $"[EditCard][TryParseAddr_fromAddrModelName] _addrModelName not null or empty", AppLogLevel.Trace);

        int l = _addrModelName.IndexOf('[');
        if(l < 0) return false;
        AppLogger.Log_To_File_log(_category, $"[EditCard][TryParseAddr_fromAddrModelName] [ found", AppLogLevel.Trace);

        int r = _addrModelName.IndexOf(']', l +1);
        if(r < 0 || r == l + 1) return false;
        AppLogger.Log_To_File_log(_category, $"[EditCard][TryParseAddr_fromAddrModelName] ] found", AppLogLevel.Trace);

        ReadOnlySpan<char> span = _addrModelName.AsSpan(l + 1, r - l - 1).Trim();
        var tmp_return_val = uint.TryParse(span, out value);
        AppLogger.Log_To_File_log(_category, $"[EditCard][TryParseAddr_fromAddrModelName] tmp_return_val = {tmp_return_val}", AppLogLevel.Trace);
        return tmp_return_val;
    }

    private void clickMachineName(uint addr, string _selectedMachineName)
    {
        string tmp_modelName = $"[{addr}] : {_selectedMachineName}";
        if(!(string.Equals(currentEdit_INFO_DataFormat_ref.MachineName, tmp_modelName, StringComparison.Ordinal)))
        {
            currentEdit_INFO_DataFormat_ref.MachineName = tmp_modelName;
            currentEdit_INFO_DataFormat_ref.Addr = addr;
            currentEdit_INFO_DataFormat_ref.Cmd = "";
            currentEdit_INFO_DataFormat_ref.Unit = "";
        }
        currentSelected_Addr = addr;

        INFO_setting_stage = INFO_SETTING_STAGE.SETTING_VALUE_AND_CMD_STAGE;
    }
    private void clickCmd(string _cmd, string _unit)
    {
        if(currentEdit_INFO_DataFormat_ref.Cmd != _cmd)
        {
            currentEdit_INFO_DataFormat_ref.Cmd = _cmd;
            currentEdit_INFO_DataFormat_ref.Unit = _unit;
        }
        

        INFO_setting_stage = INFO_SETTING_STAGE.SETTING_VALUE_AND_CMD_STAGE;
    }
    private void clickUnit(string _unit)
    {
        currentEdit_INFO_DataFormat_ref.Unit = _unit;
        INFO_setting_stage = INFO_SETTING_STAGE.SETTING_VALUE_AND_CMD_STAGE;
    }

    private void confirm_INFO_DataFormat_ValueSetting()
    {
        if( string.IsNullOrEmpty(currentEdit_INFO_DataFormat_ref.MachineName)      ||
            string.IsNullOrEmpty(currentEdit_INFO_DataFormat_ref.Cmd)              ||
            string.IsNullOrEmpty(currentEdit_INFO_DataFormat_ref.ValueName)) 
        {
            //string.IsNullOrEmpty(currentEdit_INFO_DataFormat_ref.Unit) 
            Console.WriteLine("有數值沒有設定");
            return;
        }
        
        Console.WriteLine("[currentEdit_INFO_DataItem_ref] :");
        string json = JsonSerializer.Serialize(currentEdit_INFO_DataItem_ref, new JsonSerializerOptions{
            WriteIndented = true
        });
        Console.WriteLine(json);


        INFO_setting_stage = INFO_SETTING_STAGE.ADD_VALUE_AND_CMD_ROW_STAGE;
    }
    
    private void confirm_INFO_DataFormats_Setting()
    {
        currentEdit_INFO_DataItem_ref.status = SettingStatus.DONE;
        INFO_setting_stage = INFO_SETTING_STAGE.ADD_ROW_STAGE;
    }

    private void Confirm_INFO_DataItem()
    {
        Console.WriteLine("[edit_INFO_DataItems] : ");
        string json = JsonSerializer.Serialize(edit_INFO_DataItems, new JsonSerializerOptions{
            WriteIndented = true
        });
        Console.WriteLine(json);
    
        currentCardInfo.cardName = edit_CardName;
        currentCardInfo.Info_DataItems = edit_INFO_DataItems;
        
        // 通知父元件更新 currentCardInfo
        OnCardInfoUpdated.InvokeAsync(currentCardInfo);
    }

    #endregion INFO_CARD_Variables_and_Functions
    

    #region LINK_STATUS_Variables_and_Functions
    private STATUS_SETTING_STAGE STATUS_setting_stage = STATUS_SETTING_STAGE.OVERVIEW;
    private LINK_STATUS_Pair currentEdit_LINK_STATUS_pair_ref;
    private List<LINK_STATUS_Pair> edit_Link_STATUS_Pairs = new List<LINK_STATUS_Pair>
    {

    };

    private void selectImg_LINK_STATUS(LINK_STATUS_Pair _temp_LINK_STATUS_Pair)
    {
        currentEdit_LINK_STATUS_pair_ref = _temp_LINK_STATUS_Pair;
        STATUS_setting_stage = STATUS_SETTING_STAGE.SELECT_ICON_STAGE;
    }

    private void selectedImgFile_LINK_STATUS(string _imgPath)
    {
        Console.WriteLine(_imgPath);
        currentEdit_LINK_STATUS_pair_ref.iconPath = _imgPath;
        STATUS_setting_stage = STATUS_SETTING_STAGE.OVERVIEW;
    }

    private void Confirm_LINK_STATUS()
    {
        Console.WriteLine("[edit_INFO_DataItems] : ");
        string json = JsonSerializer.Serialize(edit_INFO_DataItems, new JsonSerializerOptions{
            WriteIndented = true
        });
        Console.WriteLine(json);
        
        currentCardInfo.Link_STATUS_Pairs = edit_Link_STATUS_Pairs;

        OnCardInfoUpdated.InvokeAsync(currentCardInfo);
    }
    #endregion LINK_STATUS_Variables_and_Functions

    #region CHART_Variables_and_Functions
    private CHART_SETTING_STAGE CHART_setting_stage = CHART_SETTING_STAGE.OVERVIEW;
    private CHART_SETTING edit_ChartSetting_Clone = new CHART_SETTING();
    private CHART_SINGLE_DATA_LINE blank_singleDataLine = new CHART_SINGLE_DATA_LINE();
    private int SingleLine_index = 0;
    private MudForm form;
    private bool success;
    private string chartCMD;
    private string temp_Y_singleTitle = null;
    private string temp_chartTitle = null;
    private string chartForm_errorMessage = null;

    private void SetCMD_CHART()
    {
        CHART_setting_stage = CHART_SETTING_STAGE.SELECT_CMD_STAGE;
    }
    private void ChartDetailSubmit()
    {

    }
    private void ToggleDropdown_CHART()
    {

    }

    private void clickCmd_CHART(string _selected_Cmd)
    {
        var target_DataLine = edit_ChartSetting_Clone.chart_single_data_lines[SingleLine_index];
        target_DataLine.Cmd = _selected_Cmd;
        target_DataLine.Unit = "";
        target_DataLine.status = SettingStatus.PROGRESS;
        CHART_setting_stage = CHART_SETTING_STAGE.UNIT_SELECTION_STAGE;

        if(!string.IsNullOrEmpty(target_DataLine.Cmd))
        {
            if(cmdDict.ContainsKey(target_DataLine.Cmd))
            {
                var unitToken = cmdDict[target_DataLine.Cmd]["Unit"];
                if(unitToken != null)
                {
                    UnitLists = cmdDict[target_DataLine.Cmd]["Unit"].ToObject<List<string>>();
                }
            }
            CHART_setting_stage = CHART_SETTING_STAGE.UNIT_SELECTION_STAGE;
        }
        else
        {
            Console.WriteLine("selectUnit : 尚未選擇CMD (這邊可以跳個Alert框)");
        }
    }

    private void clickUnit_CHART(string _selected_Unit)
    {
        var target_DataLine = edit_ChartSetting_Clone.chart_single_data_lines[SingleLine_index];
        target_DataLine.Unit = _selected_Unit;
        chartForm_errorMessage = null;
        CHART_setting_stage = CHART_SETTING_STAGE.DISPLAY_SETTING;
        
    }

    private void DeleteAction_In_CHART_OVERVIEW_STAGE()
    {
        Console.WriteLine("執行刪除");
        if(SingleLine_index >= 0 && SingleLine_index < edit_ChartSetting_Clone.chart_single_data_lines.Count)
        {
            edit_ChartSetting_Clone.chart_single_data_lines.RemoveAt(SingleLine_index);
        }
    }

    private void Add_Curve()
    {
        if(edit_ChartSetting_Clone.chart_single_data_lines.Count == 2){return;}
        edit_ChartSetting_Clone.chart_single_data_lines.Add(new CHART_SINGLE_DATA_LINE
        {
            Cmd = "",
            Y_axis_selection = true,
            color = "",
            Data = null,
        });
    }

    private void Confirm_CurveSetting()
    {
        Console.WriteLine("[edit_ChartSetting_Clone] : ");
        string json = JsonSerializer.Serialize(edit_ChartSetting_Clone, new JsonSerializerOptions{
            WriteIndented = true
        });
        Console.WriteLine(json);
    
        edit_ChartSetting_Clone.ChartTitle = temp_chartTitle;

        currentCardInfo.Chart_Setting = edit_ChartSetting_Clone.DeepClone();

        currentCardInfo.cardName = temp_chartTitle; //Avoid Bug


        
        // 通知父元件更新 currentCardInfo
        OnCardInfoUpdated.InvokeAsync(currentCardInfo);
    }

    private async Task Confirm_SingleDataLine_setting()
    {
        await form.Validate();
        
        string colorError = string.IsNullOrEmpty(blank_singleDataLine.color) ? "Y-Data Color is required" : null;
        var target_DataLine = edit_ChartSetting_Clone.chart_single_data_lines[SingleLine_index];
        bool sameAxis_Flag = false;
        if(edit_ChartSetting_Clone.chart_single_data_lines.Count == 2)
        {
            int opposite_index = (SingleLine_index == 0) ? 1 : 0;
            
            sameAxis_Flag = (blank_singleDataLine.Y_axis_selection == edit_ChartSetting_Clone.chart_single_data_lines[opposite_index].Y_axis_selection) ? true : false;
        }
        
        

        if(!success || colorError != null || sameAxis_Flag == true)
        {
            Console.WriteLine("表單驗證失敗");
            target_DataLine.status = SettingStatus.PROGRESS;
            if(!success){chartForm_errorMessage = "請確認所有欄位皆已填寫";}
            else if(colorError != null){chartForm_errorMessage = "未選擇資料顏色";}
            else if(sameAxis_Flag){chartForm_errorMessage = "兩組資料不可位於同一側";}
            return;
        }
        Console.WriteLine("表單驗證成功");
        
        target_DataLine.Y_axis_selection = blank_singleDataLine.Y_axis_selection;
        target_DataLine.color = blank_singleDataLine.color;
        if(target_DataLine.Y_axis_selection == true)
        {//left axis
            edit_ChartSetting_Clone.Y_left_Title = temp_Y_singleTitle;
        }
        else if(target_DataLine.Y_axis_selection == false)
        {//right axis
            edit_ChartSetting_Clone.Y_right_Title = temp_Y_singleTitle;
        }

        Console.WriteLine($"Y-Title = {temp_Y_singleTitle}, Y-axis = {edit_ChartSetting_Clone.chart_single_data_lines[SingleLine_index].Y_axis_selection}, color = {edit_ChartSetting_Clone.chart_single_data_lines[SingleLine_index].color}");
        
        target_DataLine.status = SettingStatus.DONE;
        CHART_setting_stage = CHART_SETTING_STAGE.OVERVIEW;

        
    }
    #endregion CHART_Variables_and_Functions
    
}