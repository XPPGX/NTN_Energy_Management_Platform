@page "/Log"

@rendermode InteractiveServer
@inject IJSRuntime JS
@using demoVer.Components.Layout
@using demoVer.Models
@using demoVer.Utils
@using demoVer.Services

<MudPopoverProvider/>

@*Web Content*@
<PageTitle>Log</PageTitle>
<TopRow />

<div class="content-container">
    <div class="top-panel @(isCollapsed ? "collapsed" : "expanded")" @onclick="TogglePanel" @onclick:stopPropagation="true">
        @if(isCollapsed)
        {
            <div class="collapsed-label-only">
                條件設置
            </div>
        }
        else
        {
            <div class="panel-content">
                <!-- Step 1, Step 2, Step 3 放這 -->
                <!-- Step 1 -->
                <div class="@GetStepClass(1)" @onclick="() => GoStep(1)" @onclick:stopPropagation="true">
                    @if (currentStep == LogType)
                    {
                        <label for="logType">Log Type</label>
                        <select id="logType" @onchange="OnLogTypeChanged" @onclick:stopPropagation="true">
                            <option value="">-- Select --</option>
                            <option value="event">Event Log</option>
                            <option value="data">Data Log</option>
                        </select>

                        <div class="multi-select">
                            <p class="section-title">Parameters</p>
                            <br>
                            @foreach (var option in parameterOptions)
                            {
                                <label class="multi-option">
                                    <input type="checkbox" value="@option" 
                                        checked="@selectedParameters.Contains(option)" 
                                        @onchange="e => OnParameterChanged(option, e)" />
                                    @option
                                </label>
                            }
                        </div>
                    }
                    else
                    {
                        <div class="collapsed-label">
                            Log Type: @(selectedLogType ?? "未選擇")
                            <br/>
                            Parameters: @(selectedParameters.Count > 0 ? string.Join(", ", selectedParameters) : "未選擇")
                        </div>
                    }
                </div>

                <!-- Step 2 -->
                <div class="@GetStepClass(2)" @onclick="() => GoStep(2)" @onclick:stopPropagation="true">
                    @if (currentStep == Date)
                    {
                        <label for="startDate">Start Date</label>
                        <input type="date" id="startDate" @bind="selectedStartDate" @onclick:stopPropagation="true" @onchange:stopPropagation="true">
                        <label for="endDate">End Date</label>
                        <input type="date" id="endDate" @bind="selectedEndDate" @onclick:stopPropagation="true" @onchange:stopPropagation="true">
                        @* @onchange="@(e => OnDateChanged(0, e))"  *@
                    }
                    else
                    {
                        <div class="collapsed-label">
                            Date: @(selectedStartDate?.ToShortDateString() ?? "未選擇")
                            ~ @(selectedEndDate?.ToShortDateString() ?? "未選擇")
                        </div>
                    }
                </div>

                <!-- Step 3 -->
                <div class="@GetStepClass(3)" @onclick="() => GoStep(3)" @onclick:stopPropagation="true">
                    @if (currentStep == Btn)
                    {
                        <div class="button-row">
                            <button class="btn btn-search" @onclick="searchClick" @onclick:stopPropagation="true">查詢</button>
                            <button class="btn btn-export" @onclick="exportClick" @onclick:stopPropagation="true">匯出</button>
                        </div>
                    }
                    else
                    {
                        <div class="collapsed-label">
                            操作選項
                        </div>
                    }
                </div>
            </div>
        }
    </div>
    <div class="bottom-panel">
        <h3>下方區域</h3>
        <p>這裡放主要內容，例如表格或圖表</p>
    </div>
</div>

@code
{
    private string _category    = "";
    
    private const int LogType   = 1;
    private const int Date      = 2;
    private const int Btn       = 3;
    
    private int currentStep     = 1;
    private string? selectedLogType;
    private DateTime? selectedStartDate;
    private DateTime? selectedEndDate;
    private List<string> parameterOptions = new() {"電壓", "電流", "狀態", "轉速"};
    private HashSet<string> selectedParameters = new();
    private bool isCollapsed = false;


    protected override void OnInitialized()
    {
        _category = GetType().FullName!;
    }

    private void GoStep(int step)
    {
        currentStep = step;
        switch(currentStep)
        {
            case LogType:
                Console.WriteLine("LogType");
                break;
            case Date:
                Console.WriteLine("Date");
                break;
            case Btn:
                Console.WriteLine("Btn");
                break;
            default:
                break;
        }
    }

    private string GetStepClass(int step)
    {
        if (step == currentStep) return "step active";
        if (step < currentStep) return "step left";
        if (step > currentStep) return "step right";
        return "step";
    }

    private void TogglePanel()
    {
        if(IsMobileView())
        {
            isCollapsed = !isCollapsed;
        }
    }

    private bool IsMobileView()
    {
        return true;
    }

    private void CollapsePanel()
    {
        isCollapsed = true;
    }

    private void ExpandPanel()
    {
        isCollapsed = false;
    } 

    private void OnLogTypeChanged(ChangeEventArgs e)
    {
        selectedLogType = e.Value?.ToString();
    }

    private void OnParameterChanged(string option, ChangeEventArgs e)
    {
        bool isChecked = e.Value?.ToString()?.ToLower() == "true";

        if(isChecked)
        {
            selectedParameters.Add(option);
        }
        else
        {
            selectedParameters.Remove(option);
        }

        Console.WriteLine($"{option} => {(isChecked ? "勾選" : "取消")}");
    }

    private void OnDateChanged(int index, ChangeEventArgs e)
    {
        if (DateTime.TryParse(e.Value?.ToString(), out var dt))
        {

            if(index == 0)
            {
                selectedStartDate = dt;
                Console.WriteLine("START Time Chosen");
            }
            else if(index == 1)
            {
                selectedEndDate = dt;
                Console.WriteLine("END Time Chosen");
            }
        }
    }
    
    private void searchClick()
    {
        CollapsePanel();
    }

    private void exportClick()
    {
        CollapsePanel();
    }
}